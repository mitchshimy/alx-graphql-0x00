# Episode by ID - GraphQL Queries

This directory contains GraphQL queries that retrieve information about specific episodes from the Rick and Morty GraphQL API using the `episode(id: ID!)` field.

## Fields Requested
- id
- name
- air_date
- episode

## Files
Each pair of `.graphql` and `.json` files represents a query and response for a specific episode ID (1–4).
